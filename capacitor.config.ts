import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuo.GiornaleDiCantiere',
  appName: 'GiornaleDiCantiere',
  webDir: 'dist'
};

export default config;
